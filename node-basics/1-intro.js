const fname = "Marcely";
const age = 20;

if (age >= 16) {
  console.log(`${fname} is ${age} years old, so she can drive a car.`);
} else {
  console.log(`${fname} is ${age} years old, so she can't drive a car.`);
}

console.log(`It's my first node app`);
